# ROCm training dependencies.
#
# Install the ROCm PyTorch stack separately before this file, for example:
#   pip install --index-url https://download.pytorch.org/whl/rocm6.1 \
#     torch==2.4.1 torchvision==0.19.1 torchaudio==2.4.1
#
# Do not install the default requirements.txt on AMD/ROCm. It pins CUDA wheels
# such as torch==2.0.1, torchvision==0.15.2, onnxruntime-gpu, triton, and
# nvidia-* packages, which can replace the ROCm PyTorch build.

onnx==1.14.0
onnxruntime==1.15.1
pycocotools==2.0.7
PyYAML==6.0.1
scipy==1.13.0
onnxslim==0.1.31
opencv-python-headless==4.9.0.80
psutil==5.9.8
py-cpuinfo==9.0.0
huggingface-hub>=0.23.2,<2
safetensors==0.4.3
matplotlib
pandas
seaborn
tqdm
requests
